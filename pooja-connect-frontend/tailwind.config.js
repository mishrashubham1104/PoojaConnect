/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: 'class',
  content: [
    "./src/**/*.{js,jsx,ts,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        'divine-orange': '#FF9933',
        'divine-gold': '#D4AF37',
      },
      // Added custom spacing for mobile-friendly touch targets
      spacing: {
        'safe-top': 'env(safe-area-inset-top)',
        'safe-bottom': 'env(safe-area-inset-bottom)',
      },
      // Ensure standard breakpoints are clear
      screens: {
        'xs': '475px', // Extra small devices (large phones)
        'sm': '640px',
        'md': '768px',
        'lg': '1024px',
        'xl': '1280px',
      },
    },
  },
  plugins: [],
}